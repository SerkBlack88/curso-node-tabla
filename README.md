# Notas: 
Primer programa en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Define la base de la tabla a multiplicar               [number]
  -l, --listar   Define si se muestra o no la tabla de multiplicar
                                                      [boolean] [default: false]
  -h, --hasta    Define hasta qué número se realizará la tabla de multiplicar
                                                          [number] [default: 10]
```